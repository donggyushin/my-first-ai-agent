[project]
name = "my-first-ai-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13.2"
dependencies = [
    "openai>=1.106.1",
]

[dependency-groups]
dev = [
    "ipykernel>=6.30.1",
]
